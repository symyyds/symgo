[build]
  publish = "dist"
  command = "node scripts/build-static.js"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"

# Static response headers are defined in _headers.
# Redirect rules are defined in _redirects.
